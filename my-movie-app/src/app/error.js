"use client";

const Error = () => { // Il existe 2 props disponibles, reset et
    return (
        <div className="error">
            <h1>Erreur</h1>
            <p>Oups, une erreur s&apos;est produite.</p>
        </div>
    );
};

export default Error;